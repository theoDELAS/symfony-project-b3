{% extends 'base.html.twig' %}

{% block title %}Feed{% endblock %}
    
{% block body %}
    <div class="main-container my-5">
        <h1 class="font-bold text-3xl mb-3">Créer une publication</h1>
        {{ form_start(form) }}
            {{ form_row(form.picture) }}
            {{ form_row(form.location) }}
            {{ form_row(form.description) }}
            <button type="submit" class="bg-blue-400 text-white w-full px-3 font-medium shadow py-2 rounded-md hover:bg-blue-500">
                Publier
            </button>
        {{ form_end(form) }}
    </div>
{% endblock %}